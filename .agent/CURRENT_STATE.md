# Current state

Phase 1 automation scaffold is installed. The source of truth for reviews, queued tasks, scores and cycle reports is this `.agent/` directory. Run `npm run agent:status` for the current persisted status.
