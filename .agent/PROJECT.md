# Project

Social Factory is an Express application for managing multi-brand social-video production and distribution. The web client is served from `public/`; API modules live in `routes/` and `services/`.

The autonomous-development core writes its durable context and reports only under `.agent/`.
